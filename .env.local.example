# URL of the FastAPI backend (see /backend). For local dev this is the
# default already used as a fallback in app/page.tsx, but set it explicitly
# once you deploy the backend somewhere.
NEXT_PUBLIC_API_URL=http://localhost:8000
